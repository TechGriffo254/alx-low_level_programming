#include "main.h"

/**
 * _isupper - check  uppercase character
 * @c: the character checked
 * Return: 1 if c is uppercase, 0 otherwise not
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
