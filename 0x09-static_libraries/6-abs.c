#include "main.h"

/**
 * _abs - computes the absolute value the integer
 * @n: the int to be checked
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
