#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @x: integers  being added
 *
 * @y: integers being added
 *
 * Return: Always 0.
 *
 */
int add(int x, int y)
{
	return (x + y);
}
