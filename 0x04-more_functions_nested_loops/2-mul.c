#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply 2 integers.
 * @y: An integer to be multiplied with z
 * @z: An integer to be multipled with y
 * Return: is the result of the multiplication
 */
int mul(int y, int z)
{
	return (y * z);
}
