#include "main.h"

/**
* print_square - Prints n squares according n number of times
*
* @size: The number of squares/number of times
*
* Return: nothing because the prototype is void
*/

void print_square(int size)
{
int j, k;

if (size <= 0)
{
_putchar('\n');
}
else
{
for (j = 0; j < size; j++)
{
for (k = 0; b < size; k++)
{
_putchar(35);
}

_putchar('\n');
}
}

}
